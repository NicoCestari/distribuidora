import * as Icon from 'react-bootstrap-icons';

const CartWidget = () => {
    return (
        <div className='cart-style'>
            <Icon.CartCheckFill />
        </div>
    );
};

export default CartWidget;